<form method="post" action="{{ path('app_admin_menu_delete', {'id': menu.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce menu ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu.id) }}">
    <button class="btn"><i class="bi bi-trash-fill" style="font-size: 2rem; color: red;"></i></button>
</form>
